"""Auditoria de Pessoal — motor puro de parsing, vínculo e regras.

Biblioteca sem dependências do app (ADR 0009): roda local na calibração
(`python3 -m app.auditoria_pessoal.cli`) e pluga no FastAPI depois.
Glossário do contexto: docs/auditoria-pessoal/CONTEXT.md.
"""
