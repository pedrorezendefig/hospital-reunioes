# Lido pelo Supabase CLI local (config.toml, seção [auth.email.smtp]) quando
# `supabase start` roda em hospital-reunioes/. Só nível 3 (app local). Copie
# para supabase/.env (git-ignored) e preencha com a senha de app do Gmail.
SMTP_USER=
SMTP_PASSWORD=
