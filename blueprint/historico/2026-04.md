# Histórico — 2026-04

## 2026-04-23
- `0390564` fix(reunioes,pendencias): paginacao server-side + truncation warning + defensive labels
  Paginação client-side virou server-side real em /reuniões com `X-Total-Count`; /pendências ganha aviso "Mostrando X de Y" quando o total passa de 500 e labels defensivas para não quebrar com participantes sem nome.
- `b2d78b6` chore(quality): cleanup lint backend + ESLint frontend + pytest no CI
  Zera 781 erros do ruff (line-length 120, fixes massivos UP045/UP017/I001), adiciona ESLint flat config v9 no frontend e liga pytest no CI — gates verdes antes do primeiro ship.
- `38a9a9a` chore(scripts): ajuste em bulk_import_atas
  Ajustes pontuais no script usado pela skill `/migrar-atas`; não afeta runtime do app.
- `b3909e2` refactor(admin): campos opcionais nos schemas + ajustes UX usuarios
  `nome_completo`/`email` viram `Optional` para acomodar participantes externos vindos do STT sem email; contadores da confirmação atômica começam zerados; UX fina em admin/usuários.
- `1d3bc40` feat(reunioes): paginacao em /reunioes e limit em /pendencias
  Primeira versão da paginação (client-side, `PAGE_SIZE=15`) em /reuniões e `limit=200` em /pendências para segurar o payload enquanto a versão server-side não chega.
- `82bf271` refactor(frontend): remove rota /participantes nao utilizada
  Remove a tela `/participantes` (page+layout) e suas referências no middleware e Sidebar — cadastro hoje vive no fluxo de criação de reunião + admin.
- `d194bdb` docs(blueprint): sync inicial pos-fix do hook
  Atualiza `FLUXOS.md` e `ARQUITETURA.md` refletindo as rotas atuais do frontend (+dashboard, +admin/*, +reset-password, −participantes) e a organização do backend (admin como subpasta com sub-routers).
- `64c1c8e` fix(hooks): compatibilidade timeout no macOS no post-commit
  Adiciona `run_with_timeout()` que detecta `timeout`/`gtimeout` ou roda sem timeout — antes, no macOS sem coreutils, o hook abortava e nunca chegava a invocar `/blueprint-sync`.
- `364a002` chore(gitignore): ignora artefatos da skill /migrar-atas
  Ignora os `atas-migracao-*.json` gerados na raiz pela skill, complementando o padrão `/atas-migracao/` já existente.

## 2026-04-21
- `e6ca078` feat(admin/usuarios): tratamento granular de erros na listagem
  Substitui o empty state ambíguo por banners contextuais por status (401 sessão expirada, 403 acesso negado, 4xx/5xx servidor, erro de rede) com retry e log estruturado em `console.debug/error`, para diagnosticar sem depender de toast que some em 3s.
